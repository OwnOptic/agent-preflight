# Fixture 2 - Contract router (Copilot Studio)

Hand-authored stand-in for an exported unmanaged solution. The Copilot Studio collector reads a
solution export; until a real export exists this file set carries the same fields the collector
needs. Replace with a genuine export when one is available and the fixture should still resolve to
the same BOM.
